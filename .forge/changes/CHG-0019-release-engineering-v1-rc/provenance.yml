schema: forge/execution-provenance@2
change: CHG-0019
records:
  - id: implementation-001
    role: implementation
    execution: {id: implementation-exec-chg0019-20260820-01, context_id: implementation-context-chg0019-20260820-01}
    recorded_at: "2026-08-20T01:03:45-03:00"
    revision:
      id: chg-0019-implementation-001
      immutable_ref: {type: git_commit, value: 443678bc0ec7dbd9423d1e306416ebecdc65bdf3}
    source:
      assurance: recorded
      observed_by: self
      harness: claude-code
      statement: >-
        Self-recorded repository-native provenance for the Implementation
        execution that produced CHG-0019, continuing the same session that
        produced this Change's Intent, Discovery, Specification,
        Specification Review, Architecture, Test Strategy, and Plan/Tasks
        artifacts across this conversation, and that received explicit
        human approval of the full Discovery-through-Completion
        implementation plan before Discovery began. No provider-native
        attestation is claimed; assurance is recorded, not verified or
        cryptographic/external proof. Every RED/GREEN cycle in
        tdd-evidence.yml was observed directly by this execution via real
        pytest subprocess runs and real `python -m build` invocations
        (520 passed, 0 failed on the full suite, up from 504 on the
        pre-Implementation baseline); TDD-001's RED was captured by
        temporarily reverting the already-written pyproject.toml fix via
        `git stash`, building a real wheel with a changed CLI_VERSION
        against the unmodified file to observe genuine drift, then
        restoring the fix and rebuilding to observe genuine GREEN --
        confirmed afterward that no commit in this Change's history ever
        recorded the temporary test version value. `forge migrate --check`
        was run against this repository's own real historical data and
        confirmed to report exactly the six real candidates Discovery
        identified; `forge migrate` (the applying form) was deliberately
        never run for real against this repository's own files -- that
        remains a distinct, later, human decision, not part of this
        Change's own Completion. This Change creates no git tag, GitHub
        Release, or PyPI publication. The independent Strict Review that
        follows must run in a distinct Execution and Execution Context
        per Protocol 2 (protocol/versions/2/specification.md §2) and will
        record its own review-role provenance separately; this session
        does not and cannot produce that record itself without
        constituting self-review.
