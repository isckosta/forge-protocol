schema: forge/execution-provenance@2
change: CHG-0019
records:
  - id: implementation-001
    role: implementation
    execution: {id: implementation-exec-chg0019-20260820-01, context_id: implementation-context-chg0019-20260820-01}
    recorded_at: "2026-08-20T01:03:45-03:00"
    revision:
      id: chg-0019-implementation-001
      immutable_ref: {type: git_commit, value: 443678bc0ec7dbd9423d1e306416ebecdc65bdf3}
    source:
      assurance: recorded
      observed_by: self
      harness: claude-code
      statement: >-
        Self-recorded repository-native provenance for the Implementation
        execution that produced CHG-0019, continuing the same session that
        produced this Change's Intent, Discovery, Specification,
        Specification Review, Architecture, Test Strategy, and Plan/Tasks
        artifacts across this conversation, and that received explicit
        human approval of the full Discovery-through-Completion
        implementation plan before Discovery began. No provider-native
        attestation is claimed; assurance is recorded, not verified or
        cryptographic/external proof. Every RED/GREEN cycle in
        tdd-evidence.yml was observed directly by this execution via real
        pytest subprocess runs and real `python -m build` invocations
        (520 passed, 0 failed on the full suite, up from 504 on the
        pre-Implementation baseline); TDD-001's RED was captured by
        temporarily reverting the already-written pyproject.toml fix via
        `git stash`, building a real wheel with a changed CLI_VERSION
        against the unmodified file to observe genuine drift, then
        restoring the fix and rebuilding to observe genuine GREEN --
        confirmed afterward that no commit in this Change's history ever
        recorded the temporary test version value. `forge migrate --check`
        was run against this repository's own real historical data and
        confirmed to report exactly the six real candidates Discovery
        identified; `forge migrate` (the applying form) was deliberately
        never run for real against this repository's own files -- that
        remains a distinct, later, human decision, not part of this
        Change's own Completion. This Change creates no git tag, GitHub
        Release, or PyPI publication. The independent Strict Review that
        follows must run in a distinct Execution and Execution Context
        per Protocol 2 (protocol/versions/2/specification.md §2) and will
        record its own review-role provenance separately; this session
        does not and cannot produce that record itself without
        constituting self-review.
  - id: review-001
    role: review
    execution: {id: review-exec-chg0019-20260820-7ef99eba, context_id: review-context-chg0019-20260820-f8b709bb}
    recorded_at: "2026-08-20T01:12:21-03:00"
    revision:
      id: chg-0019-implementation-001
      immutable_ref: {type: git_commit, value: 443678bc0ec7dbd9423d1e306416ebecdc65bdf3}
      description: >-
        Independent Strict Review Iteration 1: PASS. 0 BLOCKER, 0 MAJOR,
        2 MINOR, 1 OBSERVATION -- none blocking per
        protocol/policies/review.yml. Both of this Change's highest-stakes
        claims (the migration engine's safety against this repository's
        own real historical data, and the CLI-version dynamic-sourcing
        fix) were independently reproduced, not accepted from prose.
    source:
      assurance: recorded
      observed_by: self
      harness: claude-code
      statement: >-
        Independent Reviewer Execution, run cold with no access to the
        Implementation session's conversation, deriving everything from
        committed repository state and the actual diff (git log
        d489f1b..443678b, git show on each of the four commits, git diff
        --stat). Independently reproduced: pytest -q (520 passed, matching
        the claimed figure exactly); forge validate ("Forge project is
        valid"); forge doctor (7/7 PASS plus the expected non-blocking
        WARN migration_available: 6 migration candidate(s) found...); git
        status --porcelain clean both before and after running forge
        migrate --check against this actual repository (never ran the
        applying form of forge migrate against this repository's real
        .forge/changes/ directory). Built my own independent fixtures
        (copies of the real CHG-0008 and CHG-0011 provenance.yml files,
        distinct from tests/unit/test_migration.py's own fixture list) in
        a disposable scratch directory and called find_candidates/
        apply_migrations directly: both candidates detected, both
        rewritten, and a line-by-line diff against the untouched originals
        confirmed only the schema: line differs, nothing else. Constructed
        a delegated_task fixture myself and confirmed it is never listed
        as a candidate and never touched. Verified by direct grep that
        forge/change@1 lives only in manifest.yml (never scanned by
        find_candidates, which only reads files literally named
        provenance.yml), that compatibility.md's own text forbids
        migrating forge/change@1, and that no file under src/forge_cli/
        reads or validates a project's .forge/policies/review.yml against
        either schema version. Reproduced TDD-001 end to end myself:
        changed CLI_VERSION to a throwaway 0.1.0.dev999, ran python -m
        build --wheel, confirmed the wheel filename and METADATA (Version,
        Author, all four Project-URLs, Keywords, every Classifier)
        reflected it, then reverted version.py to 0.1.0.dev0 and confirmed
        git diff was clean afterward -- no stray change left behind.
        Confirmed via git log --all -p | grep that no commit in this
        Change's history ever recorded the Implementation's own temporary
        RED-capture test value (0.1.0.dev1). Parsed all three GitHub
        Actions workflow files (publish.yml, verification.yml, tests.yml)
        with yaml.safe_load successfully; confirmed publish.yml triggers
        only on release: {types: [published]}, contains no secrets.*
        reference, and that verification.yml's stale branch triggers are
        gone with neither branch existing locally or on the remote
        (git branch -a, git ls-remote --heads origin). Confirmed C-075 is
        byte-identical (modulo wrapping) between both Contract files, that
        protocol/schemas/catalog.yml lists exactly four schema families
        with more than one version (matching Discovery's claim), and that
        no file under protocol/schemas/ or protocol/specification.md
        changed. Recorded two non-blocking MINOR findings (CHANGELOG.md
        has no dedicated entry for this Change's own substantive additions
        despite every prior historical Change having one, and no automated
        regression test guards pyproject.toml's dynamic version sourcing)
        and one OBSERVATION (apply_migrations's exact string replacement
        targets the first textual occurrence of the schema string in the
        whole file rather than being structurally scoped to the schema:
        key -- safe today, verified by direct grep that each of the six
        real candidate files contains exactly one occurrence, but a latent
        fragility for a hypothetical future file). Full findings and
        evidence recorded in review.md.
  - id: resolution-001
    role: resolution
    execution: {id: implementation-exec-chg0019-20260820-01, context_id: implementation-context-chg0019-20260820-01}
    recorded_at: "2026-08-20T01:20:49-03:00"
    scope:
      - CHANGELOG.md
      - src/forge_cli/migration.py
      - tests/unit/test_migration.py
      - tests/unit/test_packaging_version.py
    targets: [R001, R002, O001]
    revision:
      id: chg-0019-resolution-001
      immutable_ref: {type: git_commit, value: b6b570613e379d6919474240bf5a4bd36320fb1a}
      description: >-
        Resolution of review.md Iteration 1's two non-blocking MINOR
        Findings and one OBSERVATION. Same Execution/Execution Context as
        implementation-001 -- Contract C-026 requires Reviewer
        independence from Implementation, not Resolver independence, and
        review-001 (a distinct Execution/Context) already establishes
        that, matching CHG-0016/0017/0018's own resolution-001 precedent.
        R002 and O001 touch real executable behavior (a new test file,
        migration.py's replacement logic), so an independent Resolution
        Verification follows, matching CHG-0018's own judgment call for
        the same reason.
    source:
      assurance: recorded
      observed_by: self
      harness: claude-code
      statement: >-
        Self-recorded repository-native provenance for the Resolution
        execution that addressed review.md Iteration 1's R001, R002, and
        O001. scope lists the exact repository-relative paths touched,
        excluding this Change's own manifest.yml/provenance.yml/review.md
        per protocol/versions/2/specification.md SS11's Resolution Delta
        computation. R001: added a substantive CHANGELOG.md entry
        describing this Change's own additions, matching every prior
        historical Change's own convention (verified by reading the
        CHANGELOG.md structure directly before writing it). R002: added
        tests/unit/test_packaging_version.py, which reads pyproject.toml's
        actual [tool.hatch.version] pattern, applies it via `re.search`
        against the actual packaged src/forge_cli/version.py content, and
        asserts the extracted group equals CLI_VERSION -- plus a second
        test confirming the pattern is a live regex (tracks a changed
        value), not one that only happens to match today's literal
        string. O001: replaced apply_migrations's bare `str.replace` with
        a `re.compile(r"^schema: ...$", re.MULTILINE)` pattern anchored to
        the whole schema: key line, and added a new test
        (test_apply_migrations_only_touches_the_schema_key_not_a_prose_mention)
        with a fixture containing the same schema string both as the
        actual key and inside a prose `statement:` field, confirming only
        the key line is rewritten. Verified directly: pytest -q, 524
        passed (520 baseline + 4 new: 1 in test_migration.py, 3 in
        test_packaging_version.py). Did not re-verify the six real
        historical provenance files' migration behavior again in this
        Resolution -- that was already independently verified in
        review-001 and is unaffected by these three fixes (none touches
        find_candidates or the schema-family exclusion logic).
