schema: forge/change@2
protocol: 2
change:
  id: CHG-0019
  title: Release Engineering & v1 Release Candidate (Infrastructure)
  kind: feature
flow: {initial: full, current: full, escalations: []}
state: {current: in_progress}
artifacts:
  intent: complete
  discovery: complete
  specification: complete
  specification_review: complete
  architecture: complete
  test_strategy: approved
  plan: approved
  tasks: complete
  traceability: complete
  tdd_evidence: complete
  implementation: complete
  provenance: complete
  verification: complete
  review: complete
  documentation: complete
  knowledge_capture: complete
requirements: {total: 10, implemented: 10, verified: 10}
tdd: {status: compliant, cycles: 4}
verification: {status: passed}
review:
  status: passed
  iteration: 2
  blockers: 0
  majors: 0
  minors: 0
  observations: 1
  iterations:
    - id: review-001
      revision: chg-0019-implementation-001
      subject_provenance: implementation-001
      reviewer_provenance: review-001
      kind: initial_review
      status: passed
      evidence_gap: >-
        R001 (MINOR): CHANGELOG.md has no dedicated entry for this
        Change's own substantive additions (forge migrate, publish.yml,
        C-075, RELEASING.md), unlike every prior historical Change.
        R002 (MINOR): no automated regression test guards
        pyproject.toml's dynamic version sourcing (TDD-001 was verified
        only by a one-time manual build reproduction). O001
        (OBSERVATION): apply_migrations's exact string replacement
        targets the first textual occurrence of the schema string in the
        whole file rather than being structurally scoped to the schema:
        key -- safe today (verified: exactly one occurrence in each of
        the six real candidate files) but a latent fragility for a
        hypothetical future file. None blocking per
        protocol/policies/review.yml (blocking: [blocker, major]). Full
        detail in review.md.
    - id: review-002
      revision: chg-0019-resolution-001
      subject_provenance: resolution-001
      reviewer_provenance: review-002
      kind: resolution_verification
      status: passed
      full_review_required: false
      new_material_findings: 0
      evidence_gap: >-
        R001, R002, and O001 all independently re-verified resolved
        against actual repository state, not accepted from
        resolution-001's own claim: CHANGELOG.md now carries a
        substantive dedicated entry matching every prior Change's
        convention; tests/unit/test_packaging_version.py genuinely
        exercises the real hatch.version regex against the real
        version.py and would fail under a reasoned/simulated
        regression; apply_migrations now anchors its rewrite to the
        whole schema: key line via a MULTILINE regex, confirmed against
        an independently-built adversarial fixture placing a prose
        mention of the schema string both before and after the real key
        line -- only the key line was rewritten. Resolution Delta
        (git diff 443678b..b6b5706, minus this Change's own
        manifest.yml/provenance.yml/review.md) computed independently
        and matches resolution-001's declared scope exactly in both
        directions -- no Out-of-Scope Mutation. pytest -q reproduced
        independently: 524 passed. One new, unrelated, non-blocking
        OBSERVATION recorded per C-050 (O002: forge validate's C-026
        freeze check trips on review-001's earlier subject binding once
        resolution-001's real code changes post-date it -- the same
        pre-existing Core gap CHG-0018's own Iteration 2 R002 already
        documented, not a defect introduced by this Resolution). Full
        detail in review.md's "Iteration 2" section.
documentation:
  impact_evaluated: true
  update_required: true
  reason: >-
    Updated: CHANGELOG.md (PEP 440 correction, released-heading
    convention note), ROADMAP.md (version-string correction to PEP 440),
    docs/adr/0017-release-engineering-infrastructure.md,
    RELEASING.md (new), CONTRIBUTING.md (one-line pointer). No RFC
    (F-008's RFC threshold is "Material Protocol Changes"; this Change
    is CLI/tooling/CI infrastructure and one additive Contract rule, not
    a Protocol redefinition).
decisions:
  - id: DEC-001
    class: architectural
    materiality: material
    status: resolved
    authority: agent_with_review
    owning_artifact: architecture
    discovered_in: architecture
    resolved_via: autonomous_decision
