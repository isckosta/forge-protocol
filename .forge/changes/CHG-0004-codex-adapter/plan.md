# Plan — CHG-0004 Codex Harness Adapter

Status: approved

1. Descriptor and evidence: packaged identity, Protocol interval, conservative capability flags, dated evidence.
2. Projection bundle: immutable deterministic logical resources with stable digests and no required destination path.
3. Gate preservation: project canonical FULL Flow constraints into workflow content without lifecycle authority.
4. Invariant assessment: classify enforced, represented, unsupported separately from generic capabilities and reuse generic limitations.
5. Publication targets: allow only evidence-backed or explicitly configured safe destinations; otherwise bundle-only.
6. Generic Core integration: reuse planner, ownership, drift, publisher and installation state.
7. Distribution: package resources and verify isolated offline wheel without vendor SDK dependency in generic Core.
8. Verification and Strict Review: full suite, acceptance scenarios, deterministic repetition, dependency audit and adversarial review.

Codex-specific implementation belongs under `src/forge_cli/adapters/codex/`. Generic Adapter Core changes require regression-first evidence of a Core defect.