schema: forge/tdd-evidence@1
change: CHG-0018
status: compliant
cycle_count: 4
cycles:
  - id: TDD-001
    title: Core-leak fixes are pure relocation/generalization (FR-001/FR-002)
    requirements: [FR-001, FR-002, NFR-002]
    red: {observed: true, failure_reason: "ModuleNotFoundError: No module named 'forge_cli.adapters.assessment' -- python -c 'from forge_cli.adapters.assessment import assess_invariant', first run against the unmodified tree, 2026-08-19."}
    green: {observed: true, evidence: "tests/unit/test_adapter_assessment.py (renamed from test_codex_invariant_assessment.py), test_adapter_configuration.py::test_vendor_specific_prefix_is_no_longer_rejected_by_the_generic_core, test_codex_publication_targets.py::test_codex_owned_reserved_path_is_rejected -- all pass. Full suite: 437 passed (unchanged from the 437 pre-fix baseline)."}
  - id: TDD-002
    title: Claude Code Adapter projects all three mechanisms correctly (FR-003-FR-006)
    requirements: [FR-003, FR-004, FR-005, FR-006]
    red: {observed: true, failure_reason: "ModuleNotFoundError: No module named 'forge_cli.adapters.claude_code' -- pytest -q tests/unit/test_claude_code_*.py, first run before the package existed, 2026-08-19."}
    green: {observed: true, evidence: "48 new Claude Code unit tests pass, including test_hook_script_denies_in_place_mutation_of_review_control_paths (real shell subprocess execution of the generated hook script against 4 denied and 8 allowed commands) and test_claude_md_pointer_carries_the_interaction_language_directive. Also manually verified end-to-end against two real scratch Git repositories (forge init -> forge adapter install claude-code -> forge adapter doctor claude-code -> idempotent reinstall), all correct."}
  - id: TDD-003
    title: Shared conformance suite passes both drivers (FR-008/FR-009/C-074)
    requirements: [FR-008, FR-009]
    red: {observed: true, failure_reason: "Collection error: ModuleNotFoundError for forge_cli.adapters.claude_code.driver -- pytest -q tests/unit/test_adapter_driver_conformance.py, before the driver existed/was registered, 2026-08-19."}
    green: {observed: true, evidence: "test_adapter_driver_conformance.py: 15 tests (7 assertions x 2 drivers + 1 registration test) pass for both CodexDriver() and ClaudeCodeDriver(). tests/cli/test_adapter_commands.py::test_adapter_install_then_doctor_succeeds_for_every_registered_adapter (parametrized, 2 cases) passes."}
  - id: TDD-004
    title: Repository-wide forge validate / forge doctor / pytest baseline is unchanged (AC-007, CON-003)
    requirements: [AC-007, CON-003]
    red: {observed: false, failure_reason: "Compatibility regression guard, not a new-behavior assertion -- see test-strategy.md TDD-004 for the full baseline methodology."}
    green: {observed: true, evidence: "Pre-Implementation baseline (HEAD 023649a): forge validate -> 'Forge project is valid' (exit 0), forge doctor -> 7/7 PASS, pytest -q -> 437 passed. Post-Implementation: forge validate -> 'Forge project is valid' (exit 0), forge doctor -> 7/7 PASS (plus 6 new adapter:claude-code: checks when installed in a project), pytest -q (full suite) -> 504 passed. No historical Change (CHG-0001-CHG-0017) reports a new finding."}
notes:
  - "The ADR and Contract rule C-074 are not TDD cycles: Protocol SS19 marks TDD not_applicable for non-executable normative/documentation content."
  - "A real correctness gap was caught during Implementation, before any freeze: the first draft of claude_code/projection.py's _gate_instructions omitted the Protocol 2 Reviewer/Resolver-independence guidance Codex's own projection already carries (this content is harness-neutral, not Codex-specific). Found by manually cross-checking against tests/unit/test_codex_projection_gates.py while writing the parallel test_claude_code_projection_gates.py, fixed before any commit, and covered by two new tests (test_projection_bundle_renders_reviewer_resolver_independence_guidance_under_protocol_2 / ..._omits_..._under_protocol_1). See knowledge-capture.md."
