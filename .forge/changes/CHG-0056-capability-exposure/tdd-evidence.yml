schema: forge/tdd-evidence@1
change: CHG-0056
status: complete
tests:
  - id: TDD-001
    name: canonical_catalog
    command: .venv/bin/python -m pytest tests/capabilities/test_catalog.py -q
    red:
      observed: true
      failure_reason: "The catalog loader and packaged resolution did not exist before implementation."
    green:
      observed: true
      evidence: "Catalog ordering and canonical source paths pass."
  - id: TDD-002
    name: capability_skill_projection
    command: .venv/bin/python -m pytest tests/unit/test_capability_exposure_projection.py -q
    red:
      observed: true
      failure_reason: "The Codex projection had no Capability resources before implementation."
    green:
      observed: true
      evidence: "Codex generates investigate, fix, and qa skills with canonical references."
  - id: TDD-003
    name: adapter_service_projection
    command: .venv/bin/python -m pytest tests/integration/test_adapter_service.py -q
    red:
      observed: true
      failure_reason: "The Adapter projection context did not carry a Capability catalog before implementation."
    green:
      observed: true
      evidence: "37 service integration tests pass, including publication planning and ownership."
