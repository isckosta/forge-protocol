schema: forge/tdd-evidence@1
change: CHG-0016
status: compliant
cycle_count: 3
cycles:
  - id: TDD-001
    title: Codex Adapter projects protocol/artifact-structure.md by reference (FR-009/AC-009)
    requirements: [FR-009, AC-009]
    red: {observed: true, failure_reason: "TypeError: generate_codex_skill_bundle() got an unexpected keyword argument 'artifact_structure_content' -- pytest -q tests/unit/test_codex_projection_bundle.py tests/unit/test_codex_skill_projection.py, first run against the unmodified projection.py/driver.py, 2026-08-19; and AttributeError: module 'forge_cli.protocol_resolution' has no attribute 'resolve_effective_artifact_structure' -- pytest -q tests/unit/test_protocol_resolution.py, same run"}
    green: {observed: true, evidence: "test_projection_bundle_includes_artifact_structure_when_provided, test_codex_projection_includes_artifact_structure_reference_when_present, test_resolves_effective_artifact_structure_from_canonical_root, test_resolves_effective_artifact_structure_falls_back_from_versioned_root, test_fails_when_canonical_artifact_structure_is_unavailable -- all pass"}
  - id: TDD-002
    title: Adding the new resource does not change existing Adapter projection behavior (NFR-003)
    requirements: [NFR-003]
    red: {observed: false, failure_reason: "Golden-path/backward-compatibility assertion, matching CHG-0015's own TDD-001/TDD-004 pattern: the resource set for a caller that never passes artifact_structure_content is true by construction both before and after this Change (empty/absent stays absent). A meaningful RED would require the addition to leak into callers that do not opt in, which this test specifically rules out having ever observed."}
    green: {observed: true, evidence: "test_projection_bundle_omits_artifact_structure_resource_when_not_provided passes; full pre-existing suite (429 tests, including tests/integration/test_adapter_distribution.py's installed-wheel golden path) passes unchanged except two hardcoded expectations in adapter_cli_wheel_probe.py's _effective_reference_links, which were updated to include the new, intentionally-added references/artifact-structure.md link -- the one deliberate, documented behavior change, not a regression"}
  - id: TDD-003
    title: Repository-wide forge validate / forge doctor / pytest baseline is unchanged (AC-013, CON-004)
    requirements: [AC-013, CON-004]
    red: {observed: false, failure_reason: "Compatibility regression guard, not a new-behavior assertion -- see test-strategy.md TDD-003 for the full baseline methodology."}
    green: {observed: true, evidence: "Pre-Implementation baseline (HEAD 7985080): forge validate -> 'Forge project is valid' (exit 0), forge doctor -> 7/7 PASS, pytest -q tests/contract -> 35 passed. Post-Implementation: forge validate -> 'Forge project is valid' (exit 0), forge doctor -> 7/7 PASS, pytest -q (full suite) -> 429 passed, 0 failed. No historical Change (CHG-0001-CHG-0015) reports a new finding."}
notes:
  - "The prose deliverables (protocol/artifact-structure.md, Contract/Specification/Compatibility text, ARCHITECTURE.md sentence, ADR, CHANGELOG, canonical examples) are not TDD cycles: Protocol §19 marks TDD not_applicable for non-executable normative/documentation content. They are covered by Strict Review (test-strategy.md Non-mechanical Validation), not by a RED/GREEN cycle here."
